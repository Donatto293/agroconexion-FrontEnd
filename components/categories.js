import { View, Text } from "react-native";
const categories = ( ) => {
    return(
        <View className="flex-1 bg-gray-100">
            <Text className="text-blue-200">Hola Mundo desde categories</Text>
        </View>
    )
};

export default categories;